"""proofloop — test, validate and monitor agentic AI.

The proof loop: design-of-experiments test suites, fault injection, trajectory
scoring and groundedness checks (open baseline), plus GMS-backed judging and
factor attribution (optional, via the licensed knowlytix backend).

Builds on the forgeloop base library.
"""

__version__ = "0.1.3"
